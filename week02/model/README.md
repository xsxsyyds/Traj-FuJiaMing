# model/ — 行人行为模型

步骤 ② 的产出放在这里：行人使用哪些信息、如何作出判断、怎样更新速度或方向。

当前为空，依据步骤 ① 的两条行为判断（收敛—避让、绕行代价）来设计。

代码落位约定：

```
model/
├── model.py         行为模型本体：给定邻居状态 -> 输出下一步速度/方向
├── simulate.py      从真实初始状态出发推进仿真
└── README.md        模型主张与机制说明
```

`results/` 存放仿真与真实轨迹的对比图和失败情形。
